{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev": "node scripts/dev.mjs",
    "start": "pnpm --filter @workspace/api-server run start",
    "build:dvy": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/mockup-sandbox run typecheck && pnpm --filter @workspace/mockup-sandbox run build && pnpm --filter @workspace/api-server run build",
    "jobs": "pnpm --filter @workspace/api-server exec node --import tsx src/dvy/run-jobs.ts",
    "test:dvy": "node scripts/test.mjs"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3",
    "@electric-sql/pglite": "0.3.14"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10.26.0"
  }
}
